---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - PomodoroApp/AppDelegate.swift
  - PomodoroApp/UI/MenuBarView.swift
  - PomodoroApp/PomodoroApp.swift
autonomous: false
requirements: [TIMR-01, UIFP-02]

must_haves:
  truths:
    - "Menu bar shows a live countdown that ticks every second while timer is running"
    - "User can start, pause, and stop the timer from a menu bar dropdown"
    - "App launches at login via SMAppService with no user action required"
    - "Menu bar title updates immediately after sleep/wake (no stale display)"
  artifacts:
    - path: "PomodoroApp/UI/MenuBarView.swift"
      provides: "Menu bar dropdown with start/pause/stop controls and duration settings"
      contains: "MenuBarView"
    - path: "PomodoroApp/AppDelegate.swift"
      provides: "NSStatusItem title updated on every TimerEngine tick"
      contains: "statusItem"
  key_links:
    - from: "PomodoroApp/AppDelegate.swift"
      to: "PomodoroApp/Engine/TimerEngine.swift"
      via: "Combine sink on $timeRemaining to update statusItem.button.title"
      pattern: "sink.*statusItem"
    - from: "PomodoroApp/AppDelegate.swift"
      to: "SMAppService"
      via: "register() call in applicationDidFinishLaunching"
      pattern: "SMAppService.*register"
---

<objective>
Wire TimerEngine to the menu bar UI so users see a live countdown, can control the timer, and the app launches at login.

Purpose: This completes Phase 1 -- the invisible core is now visible and controllable. Users can start a focus session and watch the countdown tick in the menu bar.

Output: Live countdown in menu bar, start/pause/stop controls in menu bar dropdown, launch-at-login enabled, configurable durations.
</objective>

<execution_context>
@/Users/rod/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rod/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- TimerEngine contract the executor needs -->

From PomodoroApp/Engine/TimerEngine.swift:
```swift
@MainActor
final class TimerEngine: ObservableObject {
    enum TimerState { case idle, running, paused, onBreak }
    enum Mode { case work, break_ }

    @Published var timerState: TimerState
    @Published var timeRemaining: Int
    @Published var currentMode: Mode

    @AppStorage("workDuration") var workDuration: Int  // default 25
    @AppStorage("breakDuration") var breakDuration: Int  // default 5

    func start()
    func pause()
    func stop()
}
```

From PomodoroApp/AppDelegate.swift:
```swift
final class AppDelegate: NSObject, NSApplicationDelegate {
    var statusItem: NSStatusItem?
    // TimerEngine reference exists here
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire live countdown to menu bar and add controls</name>
  <files>
    PomodoroApp/AppDelegate.swift
    PomodoroApp/UI/MenuBarView.swift
    PomodoroApp/PomodoroApp.swift
  </files>
  <action>
1. **AppDelegate.swift** -- Add Combine observation of TimerEngine:
   - Import Combine, add `private var cancellables = Set&lt;AnyCancellable&gt;()`
   - In `applicationDidFinishLaunching`, subscribe to `timerEngine.$timeRemaining.sink` to update `statusItem?.button?.title` with formatted time (MM:SS format using `String(format: "%02d:%02d", seconds / 60, seconds % 60)`)
   - Also subscribe to `timerEngine.$timerState.sink` to prefix the title: idle shows "25:00", running shows the countdown, paused shows "II MM:SS", onBreak shows a break indicator
   - Add the NSMenu or NSPopover to the status item button that shows MenuBarView
   - Call `SMAppService.mainAppService.register()` in applicationDidFinishLaunching (auto-register, zero friction per research recommendation). Wrap in do/catch, log errors but don't crash.
   - Import ServiceManagement for SMAppService

2. **MenuBarView.swift** -- SwiftUI view for the menu bar dropdown/popover:
   - Receives TimerEngine as @ObservedObject or @EnvironmentObject
   - Shows current time remaining in large text
   - Shows current mode (Work / Break)
   - Start button (visible when idle or paused)
   - Pause button (visible when running)
   - Stop button (visible when running or paused or onBreak)
   - Work duration stepper/field (editable, bound to timerEngine.workDuration)
   - Break duration stepper/field (editable, bound to timerEngine.breakDuration)
   - Keep it minimal -- no fancy styling. Functional first.

3. **PomodoroApp.swift** -- Wire the popover/menu to display MenuBarView when the status item is clicked. Use NSPopover attached to the status item button, or NSMenu with a hosting view. NSPopover is more natural for SwiftUI content.

IMPORTANT: Use the `formatTime()` helper for all time display. The menu bar title MUST update every second while running -- verify the Combine sink fires on each tick.

For SMAppService: auto-register on first launch per user decision (zero-friction, ADHD goal). No toggle UI needed in Phase 1.
  </action>
  <verify>
    <automated>cd /Users/rod/Projects/pomodoro-app && xcodebuild build -scheme PomodoroApp -destination 'platform=macOS' 2>&1 | tail -5</automated>
  </verify>
  <done>App builds. Menu bar shows formatted time. Clicking status item opens popover with timer controls and duration settings. SMAppService.register() called on launch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live countdown and controls</name>
  <what-built>
    Complete Phase 1: timer engine with live menu bar countdown, start/pause/stop controls in a popover, configurable work/break durations, launch-at-login, agent-mode (no Dock icon).
  </what-built>
  <how-to-verify>
    1. Build and run: `cd /Users/rod/Projects/pomodoro-app && xcodebuild build -scheme PomodoroApp -destination 'platform=macOS' && open build/Build/Products/Debug/PomodoroApp.app` (or run from Xcode)
    2. Verify NO Dock icon appears
    3. Verify menu bar shows "25:00" (or similar idle state)
    4. Click the menu bar item -- verify popover opens with start/pause/stop controls and duration fields
    5. Click Start -- verify countdown ticks live in the menu bar (updates every second)
    6. Click Pause -- verify countdown stops, menu bar shows paused indicator
    7. Click Start again -- verify countdown resumes
    8. Click Stop -- verify timer resets to work duration
    9. Change work duration to 1 minute, start timer -- verify it counts down from 01:00
    10. Let it reach 00:00 -- verify it auto-transitions to break mode
    11. Check System Settings > General > Login Items -- verify PomodoroApp appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Menu bar countdown updates every second during active timer
- Start/pause/stop controls work correctly from the popover
- Work and break duration changes take effect on next session start
- Timer auto-transitions from work to break at 00:00
- No Dock icon visible
- App appears in System Settings > Login Items
- All existing TimerEngine unit tests still pass
</verification>

<success_criteria>
- Live countdown visible and ticking in menu bar while timer runs
- User can start, pause, stop timer from menu bar popover
- Work/break durations are configurable and persist
- Timer auto-transitions work -> break -> idle
- App launches at login (SMAppService registered)
- No Dock icon
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
