---
phase: 02-floating-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PomodoroApp/UI/FloatingPanelWindow.swift
  - PomodoroApp/UI/ControlsView.swift
  - PomodoroApp/AppDelegate.swift
  - PomodoroApp/UI/MenuBarView.swift
autonomous: false
requirements:
  - UIFP-01

must_haves:
  truths:
    - "Floating window appears above all other windows and fullscreen spaces"
    - "User can start, pause, stop from the floating window"
    - "Floating window does not steal keyboard focus from the active app"
    - "Panel shows live timer matching the menu bar countdown"
  artifacts:
    - path: "PomodoroApp/UI/FloatingPanelWindow.swift"
      provides: "NSPanel subclass with floating, non-activating behavior"
      contains: "nonactivatingPanel"
    - path: "PomodoroApp/UI/ControlsView.swift"
      provides: "SwiftUI timer display and controls for the panel"
      contains: "@ObservedObject var timerEngine"
    - path: "PomodoroApp/AppDelegate.swift"
      provides: "Panel lifecycle — create, show, hide, toggle"
      contains: "floatingPanel"
  key_links:
    - from: "PomodoroApp/UI/ControlsView.swift"
      to: "PomodoroApp/Engine/TimerEngine.swift"
      via: "@ObservedObject injection — same instance as menu bar"
      pattern: "@ObservedObject var timerEngine: TimerEngine"
    - from: "PomodoroApp/AppDelegate.swift"
      to: "PomodoroApp/UI/FloatingPanelWindow.swift"
      via: "stored property created in applicationDidFinishLaunching"
      pattern: "FloatingPanelWindow\\(timerEngine:"
    - from: "PomodoroApp/UI/MenuBarView.swift"
      to: "PomodoroApp/AppDelegate.swift"
      via: "Float button triggers togglePanel on AppDelegate"
      pattern: "togglePanel"
---

<objective>
Create a floating always-on-top NSPanel window with timer controls that stays visible above all apps including fullscreen spaces, without stealing keyboard focus.

Purpose: This is the primary interaction surface — users control the timer from this panel while working in other apps.
Output: FloatingPanelWindow.swift, ControlsView.swift, updated AppDelegate.swift and MenuBarView.swift
</objective>

<execution_context>
@/Users/rod/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rod/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-floating-panel/02-RESEARCH.md

@PomodoroApp/AppDelegate.swift
@PomodoroApp/UI/MenuBarView.swift
@PomodoroApp/Engine/TimerEngine.swift

<interfaces>
<!-- Key types the executor needs from existing code -->

From PomodoroApp/Engine/TimerEngine.swift:
```swift
@MainActor
final class TimerEngine: ObservableObject {
    enum TimerState: Sendable { case idle, running, paused, onBreak }
    enum Mode: Sendable { case work, break_ }

    @Published var timerState: TimerState
    @Published var timeRemaining: Int
    @Published var currentMode: Mode
    @AppStorage("workDuration") var workDuration: Int
    @AppStorage("breakDuration") var breakDuration: Int

    func start()
    func pause()
    func stop()
}
```

From PomodoroApp/AppDelegate.swift:
```swift
@MainActor
final class AppDelegate: NSObject, NSApplicationDelegate {
    var statusItem: NSStatusItem?
    var timerEngine: TimerEngine?
    private var popover: NSPopover?
    // Panel will be added as stored property here
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FloatingPanelWindow and ControlsView</name>
  <files>PomodoroApp/UI/FloatingPanelWindow.swift, PomodoroApp/UI/ControlsView.swift</files>
  <action>
Create two new files:

**FloatingPanelWindow.swift** — NSPanel subclass with these CRITICAL init-time requirements:
- Style mask MUST include `.nonactivatingPanel` in the `super.init()` call (cannot be set after init — this is a documented AppKit bug where the window server tag is not updated post-init)
- `isFloatingPanel = true` set in init body before panel is shown
- `level = .floating`
- `hidesOnDeactivate = false` (panel stays visible when app loses focus — essential because app runs in `.accessory` mode)
- `collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]` (both flags required: `.canJoinAllSpaces` makes panel appear in every Space, `.fullScreenAuxiliary` overlays fullscreen app Spaces)
- `isMovableByWindowBackground = true`
- Hide all standard window buttons (close, miniaturize, zoom) — toggle is the only show/hide mechanism
- `titleVisibility = .hidden`, `titlebarAppearsTransparent = true`
- Accept `timerEngine: TimerEngine` parameter, host `ControlsView` via `NSHostingView(rootView:)`
- `setFrameAutosaveName("FloatingPanel")` for position persistence across restarts
- Override `canBecomeKey` -> `true` (allows button interaction), `canBecomeMain` -> `false` (does not become main window)
- Content rect: `NSRect(x: 0, y: 0, width: 240, height: 200)`
- Full style mask: `[.titled, .closable, .nonactivatingPanel, .fullSizeContentView]`

**ControlsView.swift** — SwiftUI view for the panel content:
- Takes `@ObservedObject var timerEngine: TimerEngine` (NOT @StateObject — must share the existing engine instance)
- Mode label at top: "Ready" / "Working" / "Paused" / "Break" (same logic as MenuBarView.modeLabel)
- Large monospaced countdown: `.font(.system(size: 56, weight: .light, design: .monospaced))` with `.monospacedDigit()`
- Control buttons: Start (when idle/paused), Pause (when running), Stop (when running/paused/onBreak) — same conditional logic as MenuBarView
- Start button gets `.keyboardShortcut(.defaultAction)`
- `.controlSize(.large)` on the button HStack
- Padding: 20pt, frame: `minWidth: 220, idealWidth: 240, maxWidth: 300`
- Private `formatTime(_:)` and `modeLabel` helpers (same as MenuBarView)
  </action>
  <verify>
    <automated>cd /Users/rod/Projects/pomodoro-app && swift build 2>&1</automated>
  </verify>
  <done>FloatingPanelWindow.swift and ControlsView.swift exist, compile without errors, and contain all required NSPanel flags and SwiftUI controls</done>
</task>

<task type="auto">
  <name>Task 2: Wire panel into AppDelegate and add toggle to MenuBarView</name>
  <files>PomodoroApp/AppDelegate.swift, PomodoroApp/UI/MenuBarView.swift</files>
  <action>
**AppDelegate.swift modifications:**
- Add stored property: `var floatingPanel: FloatingPanelWindow?` (internal access — MenuBarView reads `isVisible` via `NSApp.delegate`)
- In `applicationDidFinishLaunching`, after existing engine setup, create the panel: `floatingPanel = FloatingPanelWindow(timerEngine: engine)` then `floatingPanel?.center()`
- Add `showPanel()` method: calls `floatingPanel?.orderFrontRegardless()` (NOT `makeKeyAndOrderFront` — that activates the app and steals focus)
- Add `hidePanel()` method: calls `floatingPanel?.orderOut(nil)`
- Add `togglePanel()` method: checks `panel.isVisible`, calls hidePanel or showPanel accordingly. This method must NOT be private — MenuBarView needs to call it via NSApp.delegate
- Keep all existing popover, status item, Combine subscription, and launch-at-login code exactly as-is

**MenuBarView.swift modifications:**
- Add a "Toggle Panel" button between the last Divider and the Quit button
- Implementation: `Button("Toggle Panel") { (NSApp.delegate as? AppDelegate)?.togglePanel() }`
- Use a static label "Toggle Panel" — simpler than trying to observe isVisible reactively
  </action>
  <verify>
    <automated>cd /Users/rod/Projects/pomodoro-app && swift build 2>&1</automated>
  </verify>
  <done>App builds successfully. AppDelegate owns the panel and exposes togglePanel(). MenuBarView has a button to toggle the floating panel.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify floating panel behavior</name>
  <action>
Human verifies that the floating panel works correctly: stays above all windows, does not steal focus, controls work, timer syncs with menu bar.
  </action>
  <verify>Manual smoke test — all 10 steps below pass</verify>
  <done>User confirms panel floats, controls work, no focus steal, timer syncs</done>
  <what-built>Floating always-on-top panel with timer controls, toggled from menu bar popover</what-built>
  <how-to-verify>
    1. Build and run: `cd /Users/rod/Projects/pomodoro-app && swift build && .build/debug/PomodoroApp`
    2. Click the menu bar timer icon to open the popover
    3. Click "Toggle Panel" — the floating panel should appear
    4. Open another app (e.g., Terminal, Safari) — the panel should stay above it
    5. Click the panel's "Start" button — verify the menu bar countdown starts AND the panel countdown matches
    6. While the timer runs, click into another app and type — verify your typing goes to that app, NOT the panel (no focus steal)
    7. Click "Pause" in the panel — verify both panel and menu bar show paused state
    8. Click "Stop" — verify both reset
    9. (Optional) Enter fullscreen in another app — verify the panel appears above the fullscreen app
    10. Close the popover and toggle the panel again to verify show/hide works
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `swift build` compiles without errors
- Existing TimerEngine tests still pass: `swift test 2>&1` (if Xcode/testing available)
- Manual smoke test: panel floats, controls work, no focus steal, timer syncs with menu bar
</verification>

<success_criteria>
- FloatingPanelWindow.swift exists with correct NSPanel init flags (nonactivatingPanel in super.init, hidesOnDeactivate=false, canJoinAllSpaces+fullScreenAuxiliary)
- ControlsView.swift exists with @ObservedObject timerEngine and start/pause/stop controls
- AppDelegate owns panel as stored property and exposes togglePanel()
- MenuBarView has button to toggle panel visibility
- App compiles and runs with floating panel visible above all windows
</success_criteria>

<output>
After completion, create `.planning/phases/02-floating-panel/02-01-SUMMARY.md`
</output>
